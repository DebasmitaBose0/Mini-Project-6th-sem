export const metadata = {
  title: 'Paraphrasing Tool',
  description: 'AI-powered paraphrasing and plagiarism analysis tool',
}

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}
